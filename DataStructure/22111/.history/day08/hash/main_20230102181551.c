#include "hash.h"




int main(int argc, char const *argv[])
{
    int arr[] = {25,51,8,};
    int len = sizeof(arr)/sizeof(arr[0]);
    
    //定义哈希表
    Node *hash[N];

    init_hash(hash);

    return 0;
}
