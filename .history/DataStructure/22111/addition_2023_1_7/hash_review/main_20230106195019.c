#include "hash.h"

int main(int argc, char const *argv[])
{
    int arr[] = {22,51,8,22,26,67,11,16,54,41};
    int len = sizeof(arr)/sizeof(arr[0]);

    //定义哈希表
    Node *hash[N];
    //初始化哈希表
    init_hash(hash);

    //将所有元素存储到哈希表中
    

    return 0;
}
